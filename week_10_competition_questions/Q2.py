
def discrete_calculus(values: list[int], change_in_y: int) -> list[int]:
    """
    Finds all indices, i, such that values[i + 1] - values[i] == change_in_y
    E.g. ([4, 4, 5, 3, 2, 3], 1) -> [1, 4]
    Because at indices 1 and 4, there is an increase of 1
    """

def discrete_squareroot(n: int) -> int:
    """
    Performs a square root. 
    if a square number, return square root
    if not, then raises a ValueError with the message
    f"{n} is not a perfect square"
    Eg. 49 -> 7
        7 -> ValueError
    CANNOT USE THE sqrt FUNCTION
    """
